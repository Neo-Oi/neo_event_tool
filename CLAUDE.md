# CLAUDE.md

## プロジェクト概要

イベント管理システム。イベントページの公開、告知、参加者の申込受付と当日受付を行う。
**扱うのは社外向けイベントのみ**（取引先・同業他社・一般の方が申込む）。
研修課題であり、技術学習の対象は IndexedDB。

成果物は単一の `index.html`。

## 参照するドキュメント

| ファイル | 内容 |
|---|---|
| `docs/要件定義書.md` | **唯一の正典。** F-01〜F-106、画面要件（S-1〜S-10）、データ設計 |
| `docs/機能一覧.md` | 画面ごとにできることの一覧 |
| `docs/画面一覧.md` | 画面の一覧（後日記載） |
| `docs/基本設計書.md` | どう実現するか（構成・画面・データ・外部IF・非機能） |
| `docs/詳細設計書.md` | 内部構造（モジュール・関数・処理フロー・改修時の注意） |

上記以外に参照すべき資料は無い。**`引き継ぎ書.md` / `タスク一覧.md` / `Peatix比較.md` は削除済み**（2026-07-30）——判断の理由は本ファイルの設計変更ログと要件定義書・詳細設計書が保持する。

---

## 技術方針

- **インターネットは使える前提**（2026-07-28 決定）。**オフライン環境での動作は要件に含めない。** 実装判断の理由に「オフラインで動くから」を使わないこと。

### 単一 HTML ファイルで完結（ビルドツール不使用）

- 成果物は `index.html` 1枚。JS と CSS はインラインに置く
- npm、バンドラ、トランスパイラ、タスクランナーは使用しない
- 外部ファイルを作らない。アイコンとプレースホルダー画像もファイル参照にせず、CDN・インライン SVG・データURI で解決する
- **配色と書体は会社サイト（`https://comthink.co.jp/`）に合わせる。** 本文 `#20272e` / アクセント `#194bf4` / 罫線 `#dee5ed`、書体は **Jost（英数字）のみ**——`@font-face` をインラインに書き、実体だけを配布元から取る。**`<link>` で外部CSSを読むのは「CSSはインライン」に反するので不可**。日本語の Zen Kaku Gothic New は約449KBあり入れられないのでシステムフォント。**状態を表す色（`--ok`/`--warn`/`--danger`）は判別しやすさを優先して別扱い**
- CDN からのライブラリ読み込みは可

### データ保存は IndexedDB（生 API、必要なら idb）

- 生の IndexedDB API を基本とする。記述量が問題になる場合のみ `idb` を CDN から読み込む
- `localStorage` と `sessionStorage` は使用しない。永続データはすべて IndexedDB に置く
- スキーマ変更は `onupgradeneeded` でバージョンを上げ、既存データを保持したままストアを追加する

### 非同期処理は Promise でラップし、呼び出し側は async/await

- IndexedDB のイベントベース API（`onsuccess` / `onerror`）は必ず Promise でラップする
- 呼び出し側にコールバックを露出させない
- データアクセスは Repository 層に集約し、全メソッドを Promise を返す形で定義する。これにより IndexedDB を実サーバ API に差し替えられる状態を保つ

### トランザクションの中で他の非同期処理を待たない

- IndexedDB のトランザクションはマイクロタスクキューが空になると自動的に閉じる
- トランザクション内で `fetch`、`setTimeout`、`FileReader` などを `await` するとトランザクションが失敗する
- 必要なデータは**トランザクションを開始する前に**すべて用意しておく
- 一括生成や複数ストアの同時更新では特に注意する

---

## 実装上の禁止事項

以下は設計の根幹に関わるため、変更しないこと。理由は下記の各項目と要件定義書の該当 F 番号に記載。

- **`Math.random()` を使わない。** トークン生成は `crypto.getRandomValues()`
- **時期（受付中・終了など）をDBに保存しない。** 締切日時と現在時刻から都度算出する
- **`applications` の `[eventId, personId]` に unique 制約を張らない。** キャンセル後の再申込を許可しているため
- **メールアドレスを認可の鍵にしない。** 申込の閲覧はランダムトークンで行う（**F-107 のログインはこれと併存する**——一覧を出すために本人確認を足しただけで、個別の申込の認可は今もトークン。`?ticket=` はログイン無しで通す）
- **`emailKey` の正規化は trim と小文字化のみ。** それ以上は別人の誤統合を招く

---

## 作業の進め方

- **全体を再生成しない。** `// ===== [n] セクション名 =====` のマーカーで区画分割し、部分更新で進める
- 動作確認はローカルHTTPサーバ経由で行う。`file://` では IndexedDB が動作しない環境がある
- DB初期化とシードデータ投入は早い段階で用意する。スキーマ変更のたびに必要になる
- 起動: `cd <このリポジトリ> && python3 -m http.server 8010` → http://127.0.0.1:8010/

---

## 設計変更ログ（2026-07-27・要件定義書より優先）

要件定義書はこの日以降、以下で上書きされている。齟齬がある場合は本ログを優先する（要件定義書の該当 F 項目はインラインで追随予定）。

- **チケット制は廃止**（Peatixの「無料チケットを買う」摩擦をなくす）。申込は「申込」に一本化し、`ticketTypes` ストアを廃止。**定員は `events.capacity` が保持**する（旧 F-61 / 4-8 を反転。イベント全体で1つの定員）。`applications` から `ticketTypeId` を除去。参加者側の「マイチケット」は「マイ申込」に改称。
- **運営（社内）機能を追加**。イベント単位で運営が使う。新ストア `messages`（`channel:'thread'|'qa'` でスレッドとQ&Aを兼用）と `tasks`（次スライスで可視化）。スキーマは **v2**（`onupgradeneeded` で `ticketTypes` を削除し新ストアを追加）。
  - **運営スレッド**（社内チャット）・**運営Q&A**（疑問の登録・解消。例:「この荷物は地下か1階か」）は **実装済み・実データで永続**。S-4 詳細のサブタブに配置（`申込 / 運営スレッド / 運営Q&A / 運営タスク / 告知 / 概要・編集`）。主催者ビュー限定（参加者ビューには出さない）。
  - **運営タスク（backlog風・E-2）も実装済み・実データで永続**（2026-07-28）。`tasks` ストア。状態は **未対応／処理中／処理済／完了** の4段（Backlog に倣う）で、イベントの「時期」（都度算出 / 4-3）とは別軸なので **保存する**。**ボード**（カンバン。ドラッグで状態変更＋並べ替え、`order` を10刻みで保持）と **タイムライン**（ガント風。今日線・週末シェード。GanttChart のコードは流用せず考え方のみ借りた）の2ビュー。**期限超過は「処理済」「完了」を除外**（処理済＝作業完了・確認待ちのため）。S-4 の固定表示にタスク進捗を1件追加。
  - **ファイル共有（E-3）は一旦後回しにしたが、2026-07-28 に実装済み**（S-4 のサブタブ／`files` ストア／F-102。詳細は後述の v5 の項）。
- **お知らせ（E-4）を追加・実装済み**（2026-07-28）。主催者→参加者の一方向の連絡手段。メールの実送信は不可能だが、Peatix のメッセージもメール通知と受信箱の2層であり、**後者に相当する層はサーバ無しでも成立する**。既存 `messages` ストアに `channel:'notice'` を足す形にしたので**スキーマ変更なし（v2 のまま）**。公開ページとマイ申込に掲示する。
- **C-1 / C-2 / C-3 すべて完了**（2026-07-28）。**モックは残っていない。** 要件定義書 1-3 の到達点（申込が実データで一周する）に到達済み。受け入れ確認 4-5 の14項目は `tests/smoke.js` の「D-4 通し」で自動検証している。
- **申込・キャンセル・チェックイン・内容修正は `AppSvc`（セクション `[4b]`）に集約**。F-25 が「主催者と参加者で処理は共通」と定めているため。**ここを迂回して `Repo.applications.put` を直接呼ばないこと。**
- **F-91〜F-97 を追加実装**（2026-07-28）: カスタム質問（F-91）／参加者名簿 S-10（F-92）／開催形式と視聴URL（F-93）／複製（F-94）／QR受付（F-95）／申込推移（F-96）／URL復帰 `?ticket=` `?event=` `?preview=`（F-97）。**新ストア・新インデックスは不要だったのでスキーマは v2 のまま。**
- **主催者タブは5つ**（ダッシュボード／イベント／通知／参加者／設定。2026-07-29 に通知を追加 / F-108）。参加者名簿はイベント横断のため独立させた（要件定義書 4-1 を改訂済み。旧「3つに固定」は無効）。
- **視聴URLは公開ページに出さない。** 申込者のマイ申込にだけ出す（F-93 / 4-3）。
- **QR生成のライブラリは `index.html` の冒頭にインライン化してある**（`qrcode-generator` / MIT）。**CDN から読まない**——理由は①第三者スクリプトの改ざんリスクを負わない（参加者の個人情報がある画面で動くため）、②テストで本物を検証できる（CDN 依存の間はスタブしか使えず、URL が404 だったのを見逃した）。**オフライン対応は理由ではない**（オフラインは要件外）。**本体とは別の `<script>` ブロック**に置き、`"use strict"` を本体だけに掛けている。`guard.js` と `smoke.js` はこの分離を前提に本体を抽出する。ライセンス原文ヘッダを消さないこと。
- **QR読み取り（`jsQR`）だけは CDN の遅延読み込み**（251KB・Apache-2.0 のため埋め込まない）。**こちらは改ざんリスクが残る**（リスク R-01）。受付は1台のみ（複数端末の同時受付は端末間共有が必要で不可能）。
- **スキーマは v3**（2026-07-28）。`readStates` を追加した（既読位置。キーは `${eventId}:${channel}`）。**未読かどうかは保存せず、最終閲覧時刻と投稿時刻から都度算出する**（時期を保存しないのと同じ考え方 / 4-3）。
- **ダッシュボードに申込ヒートマップ**（F-103）。全イベント横断・直近26週の日別申込数。**中止イベントは除外するが（F-40）、申込側のキャンセルは除外しない**（見たいのは申込という行動があった事実のため。`Domain.isValidApp` と判定を揃えないこと）。濃淡は**最大値ではなくパーセンタイル**で区切り、母集団は2件以上の日に限る（F-103b）。集計は `Dashboard` 内で完結させ、Repo/Domain の層を増やさない。
- **ダッシュボードに開催カレンダー**（F-100）。要対応リストの**下**に置く（上=今すぐやること、下=先の見通し）。**新ストア・新フィールドは無く、`events.startAt`/`endAt` から都度組み立てる**——日付索引を保存しないこと。開催日未設定は置けないので下に件数を添える。
- **イベント一覧はカード表示**（1列・全幅 / F-79）。申込バー・準備の進捗バー（運営タスクの完了率）・要対応の見出し（未読／期限超過／未対応タスク／未解決Q&A）を出す。**進捗の指標を新設せず運営タスクの完了率を流用している**——別フィールドを持つとタスクと二重管理になるため。
- **開催日時は `Util.fmtRange(startAt, endAt)` を使う**（F-46b）。同日なら終了は時刻のみ。個別に `fmtDateTime(startAt)` を書かないこと。
- **スキーマは v5**（2026-07-28）。`files`（共有ファイル / E-3・F-102）を追加。**File は Blob なのでそのまま入れる**——`FileReader` で読み直すと非同期が挟まり 7-1 の罠に近づく。1ファイル20MBまで。
- **CI がある**（`.github/workflows/test.yml`）。push と pull request で `node tests/smoke.js` を実行し、あわせて成果物の自己完結（外部ファイル参照・`localStorage`・`Math.random` の不使用）も検査する。**テストを通さない変更は CI で落ちる。**
- **CSV は数式として解釈されうる先頭文字を無害化する**（F-49b）。`=` `+` `-` `@` タブ CR で始まる値に `'` を付ける。参加者が入力した値が名簿を開いた運営側で実行されるのを防ぐ。
- **スキーマは v4**（2026-07-28）。`manuals`（イベントマニュアル。キーは `eventId`）を追加。本文が長くなるため `events` には載せない（イベント一覧が全件読むため）。
- **マニュアルの Markdown は自前描画**（`Util.markdown` / F-99c）。**外部ライブラリを使わないこと**——プレビューが既定ビューなので、CDN 不達でマニュアルが読めなくなるのを避ける。**先に `esc()` するので行頭 `>` は `&gt;` になる**。ブロック判定の正規表現はそれ前提で書く。
- **マニュアルのビューは 編集／分割／プレビュー。本文があればプレビューで開く**（読む回数の方が多いため）。初回のみ編集で開き、ひな形を挿入できる。
- **社外向けイベントのみを扱う**（F-98）。対象範囲を選ばせる項目は持たない（選択肢が1つしかないものは聞かない）。**問い合わせ先は公開時の必須項目**——社外の方は社内の連絡経路を使えず、これが唯一の連絡手段になる。参加者の所属は `persons` ではなく**カスタム質問**で取得する（所属は時期で変わるため）。**所属は「会社／学校／なし」の選択で聞く**（F-91b）——学生・個人の参加もあるため、自由記述の「会社名」にしない。
- **参加者名簿はパスワードで保護している**（F-101 / `admin`）。**これは秘匿ではなく操作の抑止**——単一HTMLではパスワードがソースに含まれ、IndexedDB も直接読める。防げるのは肩越しの覗き見と誤操作まで。**この限界の説明文は画面には出さない**（2026-07-29 改訂。旧: 画面に明記。記録は要件定義書 F-101・コード内コメントで保持する）。解除はセッション内のみ保持する。
- **要対応リストは条件だけでなく「次に何をするか」を書く**（F-31b）。「満席」とだけ出しても増席するのか案内を出すのか分からず手が止まるため。
- **受付開始日 `applyStartAt` を追加**（F-45b / 2026-07-28）。`Domain.timing()` の `phase:'before'` は宣言だけで到達不能なデッド仕様だったため解消した。任意項目で、未設定なら公開と同時に受付開始。
- **代理申込・譲渡・抽選は持たない**（F-104〜F-106 / 2026-07-28）。社外向けへの転換で需要は上がったが、名寄せキーの一意性（F-104）・本人確認不能（F-105）・通知手段が無い（F-106）を理由に維持。
- **限定公開でも SNS 共有は抑止しない**（2026-07-29 決定）。社内向けの会でもURLから入れる方が都合がよいため。F-65「限定公開は URL を知る人のみ」のまま運用する。**蒸し返さないこと**（リスク R-09 は取り下げ済み）。
- **多言語対応（英語）は一旦考えない**（2026-07-29 決定）。研修の範囲外とする。
- **参加者ビューはスマホ幅に対応する。主催者ビューはPC幅のみ**（F-81 / F-81b / 2026-07-29）。社外の参加者の端末は選べないため。
  - **`viewport` はビューごとに切り替える**（主催者 `width=1024` ／ 参加者 `width=device-width`）。切替は **`App.applyView()` の1箇所だけ**で行い、`render()` から `route.view` に追従させる。**`document.body.dataset.view` を直接書かないこと**——その経路だけ切替が漏れ、参加者の画面が 1024px 幅のまま縮小表示になる。`guard.js` が代入箇所の数を検査する。
  - **スマホ用のCSSはすべて `body[data-view="participant"]` にスコープする。** PC でウィンドウを縮めると `viewport` の指定は無視されるので、スコープを外すと主催者ビューにも当たる。スコープ漏れはテストで検査している。
  - **参加者ビューの入力欄は 16px 以上にする。** iOS Safari は 16px 未満の入力欄にフォーカスすると自動でズームし、戻らない。テストで下限を見ている。
- **マイ申込の一覧はログインが必要**（F-107 / 2026-07-29）。`persons.password` で照合（**スキーマは v5 のまま**）。
  - **これは「メールアドレスを認可の鍵にしない」の例外ではなく併存。** 個別の申込の認可は今もランダムトークンで、**確認URL（`?ticket=`）はログイン無しで通る**（メールで受け取った人はパスワードを持たないため）。ログインが要るのは一覧だけ。
  - **一覧は `personId` で引く**（以前は端末の控え `savedTokens` を読んでいたため、同じブラウザを使った別人の申込も並んでいた）。
  - **秘匿ではなく操作の抑止である。** パスワードは IndexedDB に平文で入り DevTools から読め、照合コードも手元にあるのでハッシュ化しても強度は変わらない。**この限界の説明文は画面には出さない**（2026-07-29 改訂 / F-101 と同じ扱い。記録は要件定義書 F-107b で保持する）。ログイン状態は保存しない（セッション内のみ）。
  - シードでパスワードを持つのは **田中 太郎 `abc@example.com` / `abc123`** の1人だけ。
- **カバー画像を参加者側の一覧にも出す**（F-03b / 2026-07-29）。「イベントを探す」とマイ申込のカードに帯で表示（`coverBand()`）。**シードのカバーは `Util.coverSvg()` で SVG を組み立てて Blob 化する**——画像ファイルの同梱もデータURIも使わない。色は ID から決定的（`Math.random()` 禁止）、開催形式で図柄を変える。**SVG に文字を入れないこと**（`img` で描くため書体が環境依存になる）。下書きは未設定のまま残す。**保存後の Blob は自動テストで検証できない**（fake-indexeddb が構造化複製に非対応 / R-02）ので、変更したらブラウザで表示を確認する。
- **通知（S-11 / F-108）を追加**（2026-07-29）。主催者・参加者の両ビューに「通知」タブ。**通知は保存せず、各ストアの時刻から都度組み立てる**（`Notify` / 区画 `[9g]`）——保存方式にすると全ての書き込み箇所に追記が必要になり取りこぼす。**主催者はイベント詳細9タブのうち記録が残るものすべて**（告知タブは読むだけなので対象外）、**参加者は自分が申込んだイベントの分だけ**（運営系は見せない / 4-3）。既読位置は `readStates` を再利用し**スキーマは v5 のまま**（キーは `__notify:organizer` / `__notify:participant:<personId>`）。**主催者タブは5つ・参加者タブは3つになった**（旧「4つに固定」は無効）。
- **画面の描画は直列化されている**（2026-07-29）。`App.renderContent()` が世代番号を振り、`paint(seq)` を1本の Promise 鎖に繋ぐ。①直前の描画が終わるまで次を始めない ②順番が来た時点でより新しい要求があれば捨てる。**`paint()` を直接呼ばないこと**——並行に走らせると遅い方が後から上書きする（実際にそうなっていた）。
- **「イベントを探す」には受付中・受付前のイベントだけを出す**（F-83b / 2026-07-29）。判定は `Domain.timing()` に任せ、締切と現在時刻をここで再び比べないこと（4-3）。**満席は出す**（申込は不可だが開催を知らせる価値がある）。
- **受付番号・日本語書体の同梱・オフライン対応は「対応しない」と決定済み**（2026-07-28）。理由は要件定義書の該当項目に記載。蒸し返さないこと。
- **要件定義書のインライン改訂は完了**（2026-07-28）。**以降、要件定義書と本ログの齟齬は無い**。新たな変更は要件定義書本文に直接反映すること（二重管理に戻さない）。
- **共有の前提**: 運営機能は複数人での共有管理を想定するが、サーバなし＝単一ブラウザのため**実態は一人（同一ブラウザ）で管理**。別端末との同期はサーバ導入時まで不可。
- **完成の優先順位**: **主催者／運営側の完成が第一目標。参加者側（申込フローの本実装＝C-2）は最後に仕上げる**。
