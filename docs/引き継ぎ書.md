# 引き継ぎ書

作成日：2026年7月27日

このドキュメントは、要件定義と設計を行ったチャットセッションから、Claude Codeでの実装フェーズへ引き継ぐためのものです。**Claude Code側にはこれまでの会話がないため、決定の経緯はここと要件定義書にしか残っていません。**

---

## 1. 現在地

| 工程 | 状態 |
|---|---|
| 要件定義 | ✅ 完了（要件90件） |
| 基本設計 | ✅ ほぼ完了（Repositoryのインターフェース定義のみ未着手） |
| 画面設計 | ✅ 完了（S-1〜S-9の構成・項目・挙動を決定） |
| 画面モック | ❌ S-1のみ作成済み。残り8画面は未着手 |
| 環境検証 | ❌ 未着手（**実装前に必ず通す**） |
| 実装 | ❌ 未着手 |

**次にやるべきことは第6章に記載しています。**

---

## 2. このプロジェクトは何か

イベント管理システム。イベントページの公開、告知、参加者の申込受付、当日の受付を行う。
**扱うのは社外向けイベントのみ**（2026-07-28 改訂。当初は社内向けのみを想定していたが、全面的に反転した）。

**研修課題であり、技術学習の対象は IndexedDB。** 成果物は単一の `index.html` で、サーバを持たない。

「モックアップ」ではなく**機能プロトタイプ**である。データが永続化され、状態が遷移し、検索が動作する。ただしサーバを要する機能（メール送信、端末間のデータ共有など）は対象外としている。

---

## 3. 資料の一覧

| ファイル | 役割 | 状態 |
|---|---|---|
| `CLAUDE.md` | Claude Codeが毎セッション読む技術方針 | **最新** |
| `要件定義書.md` | 唯一の正典。F-01〜F-90、画面要件、データ設計 | **最新** |
| `引き継ぎ書.md` | 本書。決定の理由と実装時の注意 | **最新** |
| `タスク一覧.md` | Backlog登録用のタスク分解 | 一部古い（第6章参照） |
| `Peatix比較.md` | Peatixに無い／ある機能の一覧と理由の4区分。本書5章・9章の統合版 | **最新**（2026-07-28） |
| `設計方針報告書.md` | 初期の調査結果と方針。上司報告用 | **古い。参考のみ** |
| `機能定義書_主催者側.md` | 運営管理（ガント・担当者・会場）の定義 | **スコープ外。将来用** |

**実装時に参照すべきは上3つだけです。** 下2つは経緯の記録であり、現在のスコープと一致しません。特に `機能定義書_主催者側.md` に書かれているガントチャートやタスク管理は、MVPに含まれません。

### 推奨するファイル配置

```
/
├── index.html          ← 成果物
├── CLAUDE.md
└── docs/
    ├── 要件定義書.md
    ├── 引き継ぎ書.md
    └── タスク一覧.md
```

---

## 4. 重要な設計判断と、その理由

**ここが本書の中心です。** 理由を知らないと、良かれと思って覆される決定が並んでいます。

### 4-1. 単一HTML・サーバなし

課題として与えられた制約。ビルドツール、npm、バンドラは使わない。JSとCSSはインライン。外部ファイルを作らない（アイコンとプレースホルダー画像も含む）。CDN参照のみ可。

**この制約から、以下が原理的に不可能**である。実装で回避しようとしないこと。

| できないこと | 影響 |
|---|---|
| 端末間のデータ共有 | 主催者と参加者は同一ブラウザ内の役割切替で表現する |
| 定時実行（cron） | 自動リマインド、締切時刻での自動状態変更 |
| メール送信・SNS自動投稿 | APIキーをHTMLに書けないため |
| 公開URL | 共有したURLを他人が開くこと、SEO、OGP |

**この4点は「サーバが必要な理由」として成果物の説明に使う。** 実装できないことを隠すのではなく、構造として示すのが本プロジェクトの評価軸のひとつ。

### 4-2. Repository層で抽象化する

データアクセスはRepository層に集約し、**全メソッドを非同期（Promise）で定義**する。

理由は2つ。IndexedDBのイベントベースAPIを呼び出し側に露出させないこと。そして**将来サーバを導入した際に、業務ロジックを変更せずAPI実装へ差し替えられる状態を保つこと**。

この構造自体が「実運用にはサーバが必要」という説明の裏付けになる。同期的な実装にすると、この主張ができなくなる。

### 4-3. 時期を保存せず、都度算出する

イベントの状態は2軸に分ける。

| 軸 | 値 | 扱い |
|---|---|---|
| 明示状態 | 下書き／公開／中止 | **保存する**（`events.status`） |
| 時期 | 受付前／受付中／受付終了／開催済 | **保存しない。** 締切日時と現在時刻から都度算出 |

定時実行がないため「締切を過ぎたら受付終了に変更する」処理が書けない。都度算出にすることで、その処理自体を不要にしている。**状態の不整合が構造的に発生しない**という副次的な利点もある。

**時期をDBに保存しようとしないこと。** 設計の根幹に関わる。

### 4-4. 状態遷移は一方向

```
下書き ──公開──→ 公開 ──中止──→ 中止（終端）
  │
  └─ 削除可（公開後は削除不可）
```

公開したら下書きに戻せず、中止も取り消せず、削除もできない。**「記録は消さない」という一貫した思想**で選択した。

その代償として、公開ボタンと中止ボタンには確認モーダルが必須（F-87）。誤操作の唯一の防波堤になる。

公開範囲（一般公開／限定公開）は `status` とは別軸。限定公開から一般公開へは変更でき、逆は不可。

### 4-5. `person` エンティティを持つ

申込（`applications`）とは別に、人（`persons`）のレコードを持ち、メールアドレスで名寄せする。

**MVPでは `persons` の画面を作らない。** それでも当初から持つ理由は、後から追加すると全 `applications` の付け替えが必要になるため。

名寄せキー `emailKey` の正規化は **trim と小文字化のみ**。それ以上（ドット除去、`+タグ` 除去など）は行わない。過度な正規化は別人を同一人物として統合する恐れがあり、誤統合の分離は困難なため。

### 4-6. 認可はランダムトークン

参加者が自分の申込を見る手段は、`crypto.getRandomValues()` で生成したトークン付きURL。

**メールアドレスを鍵にしないこと。** メールアドレスを入力すれば履歴が見られる実装は、アドレスを推測すれば他人の情報が閲覧できることを意味する。

`Math.random()` は予測可能なので使わない。

### 4-7. メールアドレスは識別子であり、連絡手段ではない

メール送信機能は持たない。メールアドレスは名寄せと本人照会にのみ使う。

同意チェックボックス（`contactConsent`）は申込フォームに置くが、**今回は使用しない**。将来メール機能を追加した際、同意を取っていない過去の参加者には送信できないため、先に取得しておく。

### 4-8. 定員は申込枠が持つ

`events` に定員を持たせず、`ticketTypes.capacity` のみを真の値とする。イベント全体の定員と残席は枠の合計から算出する。

真の値を1箇所にするため。MVPは1枠のみなので、画面上は「定員」という入力欄が1つあるだけ。

### 4-9. 参加者側は最小限

参加者向けは公開ページ・申込・完了・マイチケットの4画面のみ。**イベント検索、グループページ、フォロー機構、アクティビティ投稿は持たない。**

参加者側を「体験を提供する画面」ではなく「管理側にデータを供給する入力口」と位置づけたため。

---

## 5. 実装しないものと、その4区分

**「なぜないのか」の説明が区分ごとに違う。** 混同すると検討漏れに見える。

> 本章と第9章を統合し、Peatix との対比の形に整理したものが `Peatix比較.md`。
> 対外的な説明にはそちらを使う。理由の正典は本書のまま。

| 区分 | 該当 |
|---|---|
| **構造的に不要** | ウィジェット埋め込み、リンク先URL変更、有料集客サービス、カテゴリー、カスタムURL |
| **技術的に不可能**（サーバが必要） | メール送信、SNS自動投稿、SEO・OGP、他端末での閲覧計測、地図の埋め込み |
| **意図的に除外** | フォロワー機構、QRコード、チケット譲渡、キャンセル待ち、抽選、代理申込、定期開催、プレビュー共有、レスポンシブ対応、参加者側の発見機能（イベント検索・グループページ・アクティビティ投稿 / 4-9）、**チケット制**（2026-07-27 追加。複数チケット種別・価格設定・枠ごとの定員を持たない） |
| **対象領域外** | 決済・手数料・入金・返金・請求書、ライブ配信、マルチテナント（主催者は自社のみ） |

「ウィジェット埋め込み」が構造的に不要な理由：イベントページが自社側にあるため、外部プラットフォームへの橋渡しが発生しない。

「カテゴリー」が構造的に不要な理由：参加者側のイベント検索を持たないため、絞り込む画面が存在しない。

---

## 6. 次にやること

> **追記（2026-07-27）**: 実装フェーズに入り、A-1〜A-3 と B/C/D の可視面は実装済み（`index.html` 単一ファイル）。以降の方針は `CLAUDE.md`「設計変更ログ」が最新。**チケット廃止・運営スレッド/Q&A は実装済み**。**主催者／運営側の完成を最優先**とし、**参加者側（申込フロー本実装＝C-2）は最後に仕上げる**（下記の C-2 最優先はこの指示で上書き）。次スライスは **運営タスク可視化（backlog風・E-2）**、その後 ファイル共有（E-3）、最後に C-2。
>
> **追記（2026-07-28）**: **E-2（運営タスク）を実装完了**。`index.html` セクション `[9c]`、`tasks` ストア（スキーマは v2 のまま。既存インデックス `eventId` で足りるためバージョンは上げていない）。ボード（カンバン）とタイムライン（ガント風）の2ビューで、追加・編集・削除・ドラッグでの状態変更と並べ替えが実データで永続する。**次スライスは E-3（ファイル共有・保留中）、最後に C-2。**
>
> E-2 で決めたこと（覆す前に読むこと）:
> - **タスクの状態は保存する。** 4-3 で「保存しない」と決めたのはイベントの*時期*（締切と現在時刻から算出できるもの）。タスクの進捗はどこからも算出できないので保存する。両者は別の話。
> - **状態は 未対応／処理中／処理済／完了 の4段**（Backlog に倣う）。`tasks.status` は `todo`/`doing`/`review`/`done`。
> - **期限超過の判定から「処理済」を除外している。** 処理済＝作業は終わって確認待ち、という意味なので、赤く出すとノイズになる。数えるのは 未対応・処理中 のみ（`Domain.taskOverdue`）。
> - **並び順は `order`（列内で10刻み）。** ドロップ時に列全体を採番し直し、**変化したレコードだけ**を `Repo.bulkPut` で1トランザクション投入する（7-1 に従い、レコードは全てトランザクション開始前に組み立て済み）。
> - **ドラッグ中に `innerHTML` の全置換をしない。** 掴んだ要素が親を失い、以後の操作が画面に反映されなくなる（隣の GanttChart で実際に踏んだ罠）。`dragover` では掴んでいる要素そのものを `insertBefore` で動かし、保存と再描画は `drop` 後にまとめて行う。
> - **GanttChart からコードは流用していない。** あちらは3階層ツリー＋依存関係＋リスケジュールを持つ別物で、持ち込むと単一HTMLの規模が跳ねる。借りたのは「今日線」「週末シェード」「日付は文字列のまま扱い、加減算だけ `Date` で行う」という考え方のみ。
> - **日付は `"YYYY-MM-DD"` 文字列で保持し、比較も文字列で行う。** `new Date("2026-07-28")` は UTC 解釈になり、時差のある環境で前日にずれる。表示・範囲計算とも文字列ベースに統一している。
>
> **追記（2026-07-28・第2便）**: レビュー指摘を受けて以下を実施。
> - **C-1 完了。** イベントの書き込みが永続化された。ステップ1保存で下書きを新規作成し（F-70）、以降も「保存して次へ」ごとに保存するので離脱しても残る（F-72）。公開／中止／削除、カバー画像の Blob 保存、F-41（定員の下限）、F-66（一般公開→限定公開の禁止）、F-54 の文字数上限、F-55 の定員上限も実装。**これで残るモックは C-2（申込の確定）と C-3（チェックイン等）のみ。**
> - **受付開始日 `applyStartAt` を追加（F-45b）。** `Domain.timing()` の `phase:'before'` は**宣言だけあって到達不能なデッド仕様だった**（締切しか持っていなかったため）。受付開始日を持たせて解消し、F-08 の「受付前」も出るようにした。任意項目で、未設定なら公開と同時に受付開始。
> - **お知らせを追加（主催者→参加者の連絡手段）。** メールの実送信は不可能だが、Peatix のメッセージもメール通知と受信箱の2層であり、**後者に相当する層はサーバ無しでも成立する**。既存 `messages` ストアに `channel:'notice'` を足す形にしたので**スキーマ変更なし**（v2 のまま）。公開ページとマイ申込に掲示される。
> - **`Util.fmtDate()` の時差バグを修正。** `"YYYY-MM-DD"` を `new Date()` に渡すと UTC 解釈になり、負のオフセットの環境で前日に表示されていた（`applyDeadline` の表示が該当）。
> - **要件定義書のインライン改訂を完了。** チケット廃止の追随（F-09/18/41/61/62/63、CSV列、S-3）、S-4 のサブタブ数、F-77 の列数、3-3 のストア一覧（`messages`/`tasks`/`applications.searchText`）。**「唯一の正典」と「変更ログを優先」の二重管理を解消した。**
> - **`Peatix比較.md` の区分を見直し。** 「技術的に不可能」に ①原理的に不可能 と ②代替が成立する が混在していたため分離し、③検討していなかったもの（1-5）を新設した。**`persons` の画面が無いまま「データ蓄積が価値」と主張している矛盾も明記した。**
>
> **残る既知の未実装**（`Peatix比較.md` 1-5）: 申込フォームのカスタム質問、開催形式（オンライン/ハイブリッド）、イベント複製（`groupId`/`copiedFromId` は現在デッドフィールド）、QR受付、申込推移の可視化、参加者名簿（横断）画面。
>
> **追記（2026-07-28・第3便）— 残作業をすべて実装。モックは残っていない。**
> - **C-3 / C-2 完了。** 申込・キャンセル・チェックイン・内容修正が実データで永続する。**要件定義書 1-3 の到達点に到達した。**
> - **申込・キャンセル等は `AppSvc`（セクション `[4b]`）に集約した。** F-25 が「主催者と参加者で処理は共通」と定めているため、両方から同じ関数を呼ぶ。ここを迂回して `Repo.applications.put` を直接呼ばないこと。
> - **`applications` に `answers`（カスタム質問の回答）と `cancelledAt` を追加。** `events` に `eventFormat` / `onlineUrl` / `onlineNote` / `questions[]` を追加。**いずれも新ストア・新インデックスが不要なのでスキーマは v2 のまま。**
> - **F-91〜F-97 を要件定義書に追加**（カスタム質問・参加者名簿 S-10・開催形式と視聴URL・複製・QR受付・申込推移・URL復帰）。
> - **主催者タブを3つ→4つに変更**（ダッシュボード／イベント／**参加者**／設定）。参加者名簿はイベント横断なのでイベントタブ配下に置けない。要件定義書 4-1 を改訂済み。
> - **`groupId` / `copiedFromId` のデッドフィールドを解消。** 複製（F-94）で実際に使うようになった。
> - **受け入れ確認 4-5 の14項目をスモークテストで自動検証している**（「D-4 通し」）。手で追わなくてよい。
>
> **実装時の注意（第3便で追加した分）**
> - **QR は CDN を遅延読み込みする。** `Util.loadScript()` が使われた時だけ取得するので、オフラインでも他の機能は動く。起動時には読まない。CDN に繋がらない場合は氏名検索での受付に誘導する。
> - **複数端末での同時受付は実装できない。** QR受付は1台のみ。端末間でチェックイン状態を共有できないため（4-1）。この線引きは `Peatix比較.md` 1-2 と 1-3 に分けて書いてある。
> - **視聴URLを公開ページに出さないこと。** 申込者のマイ申込にだけ出す（F-93）。4-3 の「自分の申込は確認URL経由でのみ見える」に揃えている。
> - **申込一覧の絞り込み語はイベントを跨いだら解除する。** モジュール変数で保持しているため、`render()` で明示的にクリアしている。
>
> **残りは E-3（ファイル共有）のみ。**
>
> **追記（2026-07-28・第4便）— イベント一覧をカード表示に変更（F-79 / F-79b）。**
> 5列の表では「申込が何件か」しか分からず、**運営の準備がどこまで進んでいるかが一覧から見えなかった**。
> カードにして、1枚に次を載せている。
> - **申込バー**（申込数／定員・残席。満席は赤）
> - **準備の進捗バー**（運営タスク E-2 の完了率。タスクを進捗の代理指標として使う）
> - **要対応の見出し**（期限超過タスク／未対応タスク＝未対応＋処理中／未解決Q&A）
>
> 設計上の判断:
> - **進捗の指標を新設せず、運営タスクの完了率を流用した。** 別の進捗フィールドを持たせると
>   タスクと二重管理になり、必ずずれる。タスクが未登録なら「タスク未登録」と正直に出す。
> - **終了・中止したイベントには要対応を出さない。** 過ぎたイベントの未対応タスクを警告しても
>   行動につながらないため。カード自体も淡色（`.evcard.done`）にする。
> - 一覧で `messages` と `tasks` を全件読むようになった。件数が増えたら
>   `eventId` インデックスでの絞り込みに変えること（現状は全件でも軽い）。
>
> **追記（2026-07-28・第5便）— カード幅を全幅に戻し、未読と開始〜終了を追加。**
> - **カードは1列・全幅**（2列にしたら1件あたりが狭くなり、元の表より情報が入らなかった）。
>   横幅が広くなったぶん、申込バーと準備バーを横並びにしている。
> - **スキーマを v3 に上げ、`readStates` ストアを追加した**（F-79c）。これが
>   `onupgradeneeded` で既存データを保持したままストアを追加する初めての実例になる（3-5）。
>   キーは `${eventId}:${channel}` で、保存するのは**最終閲覧時刻だけ**。
>   **「未読かどうか」は保存しない**——投稿時刻と既読時刻から都度算出する。時期を保存しない
>   のと同じ理由で、状態の不整合が構造的に起きなくなる（4-3）。
> - 一度も開いていないイベントは全件が未読になる。これは正しい挙動なので直さないこと。
> - **未読だけは終了・中止したイベントでも表示する。** 他の要対応（期限超過など）は
>   過ぎたイベントでは行動につながらないので隠すが、読んでいない事実は変わらないため。
> - **開催日時は `Util.fmtRange(startAt, endAt)` に集約した**（F-46b）。同日なら終了は時刻だけ。
>   主催者側・参加者側の全画面がこれを使う。個別に `fmtDateTime(startAt)` を書かないこと。
> - 概要・編集タブに開催日時・開催形式・会場を追加した（従来この画面には日時が出ていなかった）。
>
> **追記（2026-07-28・第6便）— 社外向けイベントに対応し、既定を社外向けにした（F-98）。**
> - `events.audience`（`external` / `both` / `internal`）を追加。**新ストア不要なのでスキーマは v3 のまま。**
> - **既定は社外向け**（`Domain.DEFAULT_AUDIENCE`）。未設定のデータも社外向けとみなす。
>   当初の「社内向けシステム」という前提が変わったので、要件定義書 1-2 と 3-4 も改訂した。
> - **公開範囲（visibility）とは別軸。** 「誰に向けているか」と「URLを知る人だけか」は独立して決まる。
>   社外向けかつ限定公開（特定の取引先だけ）という組み合わせが実際にある。
> - **社外向け・社内外は公開時に問い合わせ先を必須にした。** 社外の方は社内の連絡経路を使えず、
>   F-64 の問い合わせ先が唯一の連絡手段になるため。
> - **対象範囲は絞り込みの軸にしない**（F-98c）。F-44 が「絞り込みは時期のみ、それ以外はバッジ」と
>   決めているので、それに従った。軸を増やすと一覧の操作が複雑になる。
> - **社外の方の所属会社は `persons` ではなくカスタム質問（F-91）で聞く。** 所属は時期で変わるうえ、
>   社内向けイベントでは不要な項目になるため、イベントごとに定義する方が実態に合う。
> - シードを社外向け中心に組み替えた（社外6・社内外1・社内2）。社外の方 p5〜p7 を追加し、
>   入館手続きや警備室への名簿提出など、社外向けで実際に増える運営タスクも入れてある。
> - ナビタブを大きくした（1つ 170px 以上・font 15px）。タブが4つになって余白が目立っていたため。

> **追記（2026-07-28・第7便）— イベントマニュアル（F-99）。スキーマ v4。**
> - `manuals` ストアを追加（キーは `eventId`。1イベント1件）。**`events` に載せなかった理由**:
>   イベント一覧は `events` を全件読むので、そこに長い本文を置くと一覧が無駄に重くなる。
> - **ビューは 編集／分割／プレビューの3つ。本文があるときはプレビューで開く。**
>   マニュアルは書く回数より読む回数の方が圧倒的に多いため。初回（空）だけ編集で開き、
>   ひな形を挿入できる。保存すると読む状態（プレビュー）へ戻す。
> - **Markdown は自前で描画している（`Util.markdown`）。外部ライブラリを使わないこと。**
>   プレビューが既定ビューなので、CDN に繋がらない環境でマニュアルが読めなくなるのは困る。
>   QR（`Util.loadScript`）は「使うときだけ」なので CDN でよいが、こちらは常時使う。
> - **先に全体を `esc()` してから組み立てる。** 本文中の HTML はタグとして解釈されない。
>   **この順序のせいで、行頭の `>`（引用）は `&gt;` になっている。** ブロック判定の正規表現は
>   `&gt;` で書く必要がある（実際にここで一度バグらせた）。記法を足すときは同じ罠に注意。
> - リンクは `http(s):` のみ許可している（`javascript:` を通さない）。
> - 分割ビューでは入力のたびにプレビューだけを差し替える。**`textarea` を作り直さないこと**——
>   `innerHTML` ごと置き換えるとカーソル位置が飛ぶ。
> - **参加者には出さない。** 運営が見る手順書であり、4-3 の役割による見え方に従う。

> **追記（2026-07-28・第8便）— 開催カレンダー（F-100）とナビタブの中央揃え。**
> - ダッシュボードの**要対応リストの下**に月カレンダーを置いた。要対応（今すぐやること）を
>   上、カレンダー（先の見通し）を下、という並びは意図的。順序を入れ替えないこと。
> - **開催日から終了日までの各日に帯を出す。** 複数日にまたがるイベントがあるため。
> - **開催日が未設定のイベントはカレンダーに置けない。** 消えたように見えるのを避けるため、
>   下に件数と一覧を添えている（F-51 が一覧で先頭に集めているのと同じ配慮）。
> - 表示中の月は `Dashboard` のモジュール変数で保持する。`render()` は月を送るたびに
>   全体を読み直すが、データ量が小さいので問題にならない。件数が増えたら月範囲で
>   絞る余地がある。
> - **新しいストアもフィールドも増やしていない。** カレンダーは `events.startAt` /
>   `endAt` から都度組み立てる。日付ごとの索引を保存しようとしないこと（4-3 と同じ考え方）。
> - ナビタブは中央揃えにした（F-100 の改訂で 4-1 に追記済み）。

> **追記（2026-07-28・第9便）— 社外向け専用に確定、要対応リストの作り替え、名簿のロック。**
> - **対象範囲（`events.audience`）を廃止した。** 一度は 社外向け／社内外／社内向け の3択に
>   したが、扱うのが社外向けだけになったので**選択肢が1つしかない項目を聞くのをやめた**。
>   バッジも入力欄も削除済み。シードも全イベントを社外向けに書き換えた（参加者8名は全員社外）。
> - **問い合わせ先を公開時の必須項目にした。** 社外の方は社内の連絡経路を使えず、
>   F-64 が唯一の連絡手段になるため。以前は社外向けのときだけ必須だったが、常時必須になった。
> - **所属会社は `persons` に持たせない。** 所属は時期で変わるので、イベントごとの
>   カスタム質問（F-91 / `q_comp`）として取得する。名簿に固定列で持ちたくなっても戻さないこと。
> - **要対応リストを表形式からカード形式に変えた（F-31b）。** 「満席」とだけ出しても
>   増席するのか案内を出すのか分からないので、**次に何をするかを本文として書く**。
>   あわせて開催日時・申込／定員・残作業（期限超過／未対応タスク／未解決Q&A）を添える。
>   緊急度で並べ替える（本日開催・満席 ＞ 明日開催・定員間近 ＞ 下書き放置）。
> - **参加者名簿にパスワードを付けた（F-101 / `admin`）。**
>   **これは秘匿ではなく操作の抑止である。** 単一HTMLなのでパスワードはソースに書かれており、
>   DevTools から IndexedDB を直接開くこともできる。防げるのは肩越しの覗き見と誤操作まで。
>   **画面にこの限界を明記してあるので消さないこと**——書かないと、保護されていると誤解される。
>   `Peatix比較.md` 1-5 の i で「クライアント側パスワードは秘匿の役に立たない」と書いた立場と
>   矛盾しないよう、あくまで抑止目的だと位置づけている。
>   解除はセッション内のみ（`Roster` のモジュール変数）。再読込と参加者ビューへの切替で再ロック。
>
> **追記（2026-07-28・第10便）— シードを過去アーカイブ中心に約4倍へ（F-33b）。**
> イベント10→40件（過去31・これから6・日程未定2・中止4）、参加者8→24名、申込220件。
> 期間は約2年（2024/09〜）。
> - **増やした理由。** 「Peatix は30日で消えるが本システムは保持する」という 2 章の主張は、
>   データが溜まっていない状態では確認しようがない。名簿の履歴・申込推移・カレンダーの
>   月送り・イベント一覧の「終了・中止」トグルも、件数が無いと動作を確かめられなかった。
> - **`Math.random()` は使っていない**（禁止事項）。添字から決定的に組み立てているので、
>   何度投入しても同じ構成になる。再現性が要るテストからも扱いやすい。
> - リピーターが出るよう人数プールを回している（最多11回参加）。名簿の「参加◯回・来場◯回」が
>   埋まるのはこのため。
> - 過去イベントには運営タスク・スレッドを付けていない。終わったイベントに要対応を出しても
>   意味がなく（カード側で `done` は要対応を隠す）、件数だけ膨らむため。





タスク一覧（`タスク一覧.md`）のA-2は完了済み。**A-3の内容は「画面モックの作成」だが、設計そのものは完了しているため、モックは描画作業のみ**になっている。

| # | 内容 | 状態 |
|---|---|---|
| **A-1** | IndexedDBスパイクと動作環境の確定 | ❌ **最優先** |
| A-2 | 状態遷移とデータモデルの確定 | ✅ 完了 |
| A-3 | 画面モックの作成（残8画面） | ❌ 設計は完了、描画のみ |
| B以降 | 実装 | ❌ |

### A-1（スパイク）を最初に通す理由

環境で動かなければ他のすべてが無駄になる。**成果物ではない使い捨てコード**で、確認するのは4点のみ。

1. ローカルHTTPサーバ経由でDBが開けるか
2. Blobを保存・読み出して `<img>` に表示できるか
3. `onupgradeneeded` でバージョンを上げてストアを追加できるか
4. DB全削除→再作成のリセットが動くか

**`file://` で直接開かないこと。** IndexedDBが利用できない、または不安定なブラウザが存在する。`python -m http.server` などを経由する。

---

## 7. 実装時の注意

### 7-1. IndexedDBの罠

**トランザクション内でIndexedDB以外の `await` を挟むとトランザクションが失敗する。** マイクロタスクキューが空になると自動的に閉じるため。`fetch`、`setTimeout`、`FileReader` などが該当する。必要なデータはトランザクション開始前にすべて用意する。

初見でほぼ確実に踏み、原因が分かりにくい。特にシードデータの一括投入で発生しやすい。

### 7-2. 一意制約を張ってはいけない箇所

`applications` の `[eventId, personId]` に unique を張らないこと。**キャンセル後の再申込を許可している**ため、同一人物が同一イベントに複数レコードを持つ。

### 7-3. コードの構成と更新方法

単一HTMLは2,000〜4,000行規模になる見込み。**全体を再生成しないこと。**

`// ===== [n] セクション名 =====` のマーカーで区画分割し、以降は部分更新で進める。全体出力を繰り返すと、無関係な箇所が壊れる、出力が途中で切れる、差分が追えないという問題が起きる。

### 7-4. 開発補助は最初に作る

DB初期化とシードデータ投入は、**スキーマ変更のたびに必要になる**ので早い段階で用意する。これがないと、古いスキーマのDBが残って動かない状態を毎回手作業で潰すことになる。

### 7-5. 検索の実装

`applications` に正規化済みの検索用文字列（氏名・カナ・メールを連結して正規化したもの）を保持する。入力のたびに全件を正規化し直さないこと。

正規化は `NFKC` → 小文字化 → カタカナをひらがなへ → 空白除去 の順。

### 7-6. 最優先マイルストーン

**公開ページ → 申込 → マイチケット → 主催者側の申込一覧に反映**、という一周が繋がった時点で提出可能な状態になる。ここを最優先で確保し、告知・ダッシュボード・設定画面は後回しでよい。

特に「ブラウザを再読込してもデータが残る」と「役割を切り替えるとデータが共有されている」の2つが、本システムの技術的な核心の証明になる。

---

## 8. 未確定事項

| 項目 | 備考 |
|---|---|
| Repositoryのインターフェース定義 | メソッド名・引数・戻り値。実装の直前に決める |
| エラーメッセージの具体的な文言 | 表示方法はF-84〜F-87で確定済み |
| 対象ブラウザの具体的な範囲 | A-1のスパイクで確定させる |
| `file://` における各ブラウザのIndexedDB挙動 | 同上 |
| 個人情報の利用目的の記載文面 | 法務確認が必要。研修では省略可 |

---

## 9. 補足：Peatixとの関係

> 本章と第5章を統合した一覧が `Peatix比較.md`。「Peatixにあって本システムに無いもの」と
> 「その逆」を1枚にまとめてある。

要件の骨格は、既存サービスであるPeatixの主催者向け機能を調査して組み立てた。公式ヘルプの記事を読み、以下の構造的制約を確認している。

- API連携が存在しない（CSV手動出力のみ）
- フォーム取得データがイベント終了30日後に自動削除される
- 参加者リストにメールアドレスが含まれない

**これらを解消し、参加者データを蓄積して次回に活用できる状態を作ることが本システムの価値**である。`person` エンティティを中核に置いているのはこのため。

イベント作成の5ステップ構成（F-69）もPeatixに倣っている。項目数の多い画面の分割方法として実績があるため。

一方、Peatixの制約のうち以下は理由が当てはまらないため採用していない。

| Peatix | 本システム | 理由 |
|---|---|---|
| 参加者リストのメールアドレス非開示 | 開示する | 第三者主催者を保護するためのマルチテナント固有の制約 |
| フォームデータ30日削除 | 保持する | データ蓄積が本システムの価値そのもの |
| チケット名の変更禁止 | 変更可 | 金銭を扱わないため、購入済み商品の同一性を保つ必要がない |
